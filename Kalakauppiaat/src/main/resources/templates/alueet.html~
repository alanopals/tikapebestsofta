<!DOCTYPE html SYSTEM "http://www.thymeleaf.org/dtd/xhtml1-strict-thymeleaf-4.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:th="http://www.thymeleaf.org">

    <head>
        <title>Foorumi</title>
        <meta charset="utf-8" />
        <style>
            table{
                width: 100%;    
            }
            th, td {
                padding: 5px;
                text-align: left;
            }

        </style>

    </head>

    <body>
        
        <br/>
        
        <table>
            <tr>
                <th>Alue</th>
                <th>Viestejä yhteensä</th>		
                <th>Viimeisin viesti</th>
            </tr>
            <tr th:each="alue: ${alueet}">
                <td><a th:href="@{'~/alue?id=' + ${alue.id} + '&amp;sivu=1'}" th:text='${alue.nimi}'>nimi</a></td>
                <td th:text="${alue.viestien_lkm}">viestien maara</td>
                <td th:text="${alue.pvm}">Viimeisin paiva</td>
            </tr>

        </table>
        
        <br/>

        <form method="POST" action="/">
            Uuden alueen nimi:<br/>
            <input type="text" name="nimi"/><br/>
            <input type="submit" value="Lisää alue"/>
            
        </form>

    </body>
</html>